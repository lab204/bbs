db:
  image: mongo:3.1.6
  environment:
    - AUTH=no
  ports:
    - "27017"

redis:
  image: redis:3.0.3
  environment:
    - REDIS_PASS=none
  ports:
    - "6379"

web:
  build: .
  ports:
    - "3000:9292"
  command: bundle exec rackup -s Puma
  links:
    - db
    - redis
